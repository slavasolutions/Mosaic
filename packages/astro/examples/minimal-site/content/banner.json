{
  "text": "Demo site. Every page below is a JSON file (plus optional Markdown) in a folder. Astro compiled it to static HTML — no runtime, no database. This banner is itself a Mosaic record: content/banner.json. It sits outside pages/, so it has no URL, but the loader still emits it and the layout reads it.",
  "linkText": "See the spec →",
  "linkUrl": "https://github.com/slavasolutions/mosaic"
}
