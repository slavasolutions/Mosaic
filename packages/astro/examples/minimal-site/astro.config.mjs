import { defineConfig } from 'astro/config';

export default defineConfig({
  // No special config; the loader is wired up in src/content.config.ts
  // (Astro 5) / src/content/config.ts (Astro 4.14+).
});
