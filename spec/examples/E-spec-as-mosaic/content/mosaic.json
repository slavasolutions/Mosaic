{
  "title": "Mosaic Base Format Specification — encoded as Mosaic",
  "version": "0.9.1-draft",
  "description": "Dogfood: the Mosaic spec stored as a Mosaic folder. Every section is a record; every cross-reference is a ref:. Proves the format describes the kind of document the format itself is.",
  "record_types": {
    "spec-section": {
      "fields": ["title", "section_number", "see_also"],
      "cascading_keys": ["locale", "spec_version"]
    },
    "spec-root": {
      "fields": ["title", "version", "abstract"]
    },
    "decision": {
      "fields": ["title", "decision_number", "status", "see_also"]
    }
  }
}
