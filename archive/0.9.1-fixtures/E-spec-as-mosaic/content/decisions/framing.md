# Decision 0 — framing (LOCKED: confirmed)

> A record is an identity. The JSON describes it. The content file is its
> body.

`about.json` is not "the about record" — it is *the JSON that describes the
record at identity `about`*. This single sentence is what makes the file-form
vs folder-form discussion (`ref:format/naming`) consistent.
