A single record. Title and lede on the JSON side, prose on the markdown side. Nothing else.

> No author card. No avatar. No theme. Just the structure that ships with the base format.

The point isn't that minimalism is better — the point is that nothing here is mandatory. Add tokens, add team records, add a banner — the spec doesn't care which you do or don't.
