{
  "name": "mosaic",
  "version": "0.9.4",
  "private": true,
  "description": "Mosaic — a folder format for structured content. Monorepo: spec, reference reader, framework adapters.",
  "license": "Apache-2.0",
  "homepage": "https://github.com/slavasolutions/mosaic",
  "repository": {
    "type": "git",
    "url": "https://github.com/slavasolutions/mosaic.git"
  },
  "workspaces": [
    "packages/*",
    "packages/astro/examples/minimal-site",
    "packages/next/examples/minimal-next-site"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "validate-spec": "python3 spec/tools/validate.py spec/examples/A-identity/content; python3 spec/tools/validate.py spec/examples/B-sidecars/content; python3 spec/tools/validate.py spec/examples/C-cascade/content; python3 spec/tools/validate.py spec/examples/D-web/content"
  },
  "engines": {
    "node": ">=20"
  }
}
